<%= hidden_field "payment_source[#{payment_method.id}]", 'order_id', value: @order.id %>
<div class="span3">
		<p>
			<%= radio_button_tag "order[instalments]", 1, true%>
			<%= "#{number_to_currency(@order.total)} a vista" %>
		</p>
</div>
